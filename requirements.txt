# LocalGuard Antivirus - Python dependencies
#
# Core (all platforms, used by engine / quarantine / database):
psutil>=5.9.0

# Windows-only (used by protection monitoring and shell integration):
pywin32>=306; sys_platform == "win32"
watchdog>=3.0.0

# System tray icon (spec section 43). Optional at runtime: the app runs
# without it and simply skips tray integration.
pystray>=0.19.0; sys_platform == "win32"
pillow>=10.0.0; sys_platform == "win32"

# Optional: YARA rule scanning. LocalGuard works fully without it.
# Install manually if you want YARA support:  pip install yara-python
yara-python>=4.3.0; sys_platform == "win32" and python_version >= "3.12"

# Development / testing:
pytest>=8.0.0

# Packaging (build-time only, not needed at runtime):
# pyinstaller>=6.0.0
